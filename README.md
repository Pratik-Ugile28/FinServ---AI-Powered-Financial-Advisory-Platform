# FinServ

This is a personal finance web app. Before running locally, copy `.env.example` to `.env` and fill in real secrets (do not commit `.env`).

Quick start:

```bash
cp .env.example .env
# edit .env with real values
npm install
npm start
```

Sensitive keys should never be committed. `.env` is listed in `.gitignore`.